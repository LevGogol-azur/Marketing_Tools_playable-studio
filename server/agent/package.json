{
  "name": "playable-agent-runner",
  "private": true,
  "type": "module",
  "description": "Runs inside the sandbox container: executes the Claude agent against /work.",
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.191",
    "tsx": "^4.19.2"
  }
}
