# Copy to .env and fill in. .env is gitignored.

# OAuth token for the Claude agent (generate once on the host with `claude setup-token`).
# Requires a Claude Pro/Max/Teams/Enterprise subscription. Lives ~1 year.
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...
